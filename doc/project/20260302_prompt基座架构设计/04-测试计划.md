# Prompt 基座项目 - 测试计划

## 测试目标

确保架构重构后，现有功能不受影响，新功能正常工作。

---

## 测试分类

### 1. 回归测试（Regression Testing）
验证现有功能是否仍然正常工作

### 2. 集成测试（Integration Testing）
验证新旧系统是否能正常协作

### 3. 功能测试（Functional Testing）
验证新功能是否按预期工作

---

## 测试清单

### 阶段 0-2：基础设施测试（当前阶段）

#### ✅ T1：目录结构验证
**目标**：确认新目录已创建，旧目录未被破坏

**测试步骤**：
```powershell
# 1. 检查新目录是否存在
Test-Path ".kiro/modules/ai-dev/v1.0"
Test-Path "doc/personal"
Test-Path "req_exploration/rfcs"
Test-Path "scripts"

# 2. 检查旧目录是否仍然存在
Test-Path ".kiro/steering/ai-dev.md"
Test-Path ".kiro/ai-dev-script.py"
Test-Path ".kiro/ai-dev-config.json"
```

**预期结果**：所有路径都返回 `True`

**实际结果**：
- [ ] 待测试

---

#### ✅ T2：配置文件验证
**目标**：确认 kiro.yaml 格式正确，可被解析

**测试步骤**：
```powershell
# 1. 验证 YAML 格式
python -c "import yaml; yaml.safe_load(open('.kiro/kiro.yaml'))"

# 2. 检查必需字段
python -c "
import yaml
config = yaml.safe_load(open('.kiro/kiro.yaml'))
assert 'active_modules' in config
assert 'module_path_template' in config
assert 'settings' in config
print('✅ 配置文件格式正确')
"
```

**预期结果**：无错误，输出 `✅ 配置文件格式正确`

**实际结果**：
- [ ] 待测试

---

#### ✅ T3：模块加载器功能测试
**目标**：验证模块加载器的核心功能

**测试步骤**：
```powershell
# 1. 列出所有模块
python scripts/module_loader.py list

# 2. 查看模块信息
python scripts/module_loader.py info --module ai-dev

# 3. 列出模块版本
python scripts/module_loader.py versions --module ai-dev

# 4. 测试错误处理（查询不存在的模块）
python scripts/module_loader.py info --module non-existent-module
```

**预期结果**：
- 命令 1-3：正常输出
- 命令 4：显示错误信息，不崩溃

**实际结果**：
- [x] 命令 1：✅ 通过（已在阶段 2 测试）
- [x] 命令 2：✅ 通过（已在阶段 2 测试）
- [ ] 命令 3：待测试
- [ ] 命令 4：待测试

---

#### ⚠️ T4：现有 ai-dev 功能回归测试
**目标**：确认旧的 ai-dev 脚本仍然可以正常运行

**测试步骤**：
```powershell
# 1. 检查脚本是否存在
Test-Path ".kiro/ai-dev-script.py"

# 2. 测试脚本帮助信息
python .kiro/ai-dev-script.py --help

# 3. 测试参数验证（不实际创建项目）
python .kiro/ai-dev-script.py --project-type "actual-project" --project-name "test-project" --date "2026-03-02" --status "pending-test" --module "automation-tools" --version "v1.0" --help
```

**预期结果**：
- 命令 1：返回 `True`
- 命令 2：显示帮助信息
- 命令 3：显示帮助信息（因为有 --help）

**实际结果**：
- [ ] 待测试

**⚠️ 风险**：如果此测试失败，说明现有功能已被破坏，需要立即修复

---

#### ⚠️ T5：ai-dev 完整流程测试（可选）
**目标**：验证 ai-dev 的完整功能链路

**前置条件**：
- 已配置 GITHUB_TOKEN
- 网络连接正常

**测试步骤**：
```powershell
# 1. 在 Kiro 中输入测试指令
# /ai-dev 新建实际项目：自动化工具类，测试项目 v0.1，状态开发中

# 2. 复制 Kiro 生成的命令并执行
# python '.kiro/ai-dev-script.py' --project-type "actual-project" --module "automation-tools" --project-name "test-project" --version "v0.1" --date "2026-03-02" --status "in-development"

# 3. 验证结果
Test-Path "01_actual-project-lib/automation-tools/automation-tools-test-project-v0.1-in-development"
Test-Path "01_actual-project-lib/automation-tools/automation-tools-test-project-v0.1-in-development/README.md"

# 4. 清理测试数据
Remove-Item -Recurse -Force "01_actual-project-lib/automation-tools/automation-tools-test-project-v0.1-in-development"
```

**预期结果**：
- 项目目录创建成功
- README.md 生成成功
- Git 仓库初始化成功
- GitHub 仓库创建成功（如果配置了 Token）

**实际结果**：
- [ ] 待测试

**⚠️ 注意**：此测试会创建真实的 GitHub 仓库，测试后需要手动删除

---

### 阶段 3：文件迁移测试（下一阶段）

#### ⏸️ T6：文件迁移完整性测试
**目标**：确认所有文件已正确迁移

**测试步骤**：
```powershell
# 1. 检查新位置文件是否存在
Test-Path ".kiro/modules/ai-dev/v1.0/steering/ai-dev.md"
Test-Path ".kiro/modules/ai-dev/v1.0/scripts/ai-dev-script.py"
Test-Path ".kiro/modules/ai-dev/v1.0/configs/ai-dev-config.json"

# 2. 检查文件内容是否一致（使用文件哈希）
$oldHash = Get-FileHash ".kiro/ai-dev-script.py" -Algorithm MD5
$newHash = Get-FileHash ".kiro/modules/ai-dev/v1.0/scripts/ai-dev-script.py" -Algorithm MD5
$oldHash.Hash -eq $newHash.Hash
```

**预期结果**：
- 所有新位置文件存在
- 文件内容与旧文件一致

**实际结果**：
- [ ] 待测试

---

#### ⏸️ T7：迁移后功能测试
**目标**：确认迁移后 ai-dev 功能仍然正常

**测试步骤**：
```powershell
# 1. 使用新路径测试脚本
python .kiro/modules/ai-dev/v1.0/scripts/ai-dev-script.py --help

# 2. 测试完整流程（同 T5）
# ...
```

**预期结果**：与 T5 相同

**实际结果**：
- [ ] 待测试

---

### 阶段 4：自动化脚本测试（未来）

#### ⏸️ T8：提示词版本管理测试
**目标**：验证 prompt_version_manager.py 功能

**测试步骤**：
```powershell
# 待编写
```

---

#### ⏸️ T9：RFC 自动记录测试
**目标**：验证 rfc_recorder.py 功能

**测试步骤**：
```powershell
# 待编写
```

---

## 测试执行记录

### 当前阶段测试（阶段 0-2）

| 测试编号 | 测试名称 | 状态 | 执行时间 | 结果 | 备注 |
|---------|---------|------|---------|------|------|
| T1 | 目录结构验证 | ⏸️ 待执行 | - | - | - |
| T2 | 配置文件验证 | ⏸️ 待执行 | - | - | - |
| T3 | 模块加载器功能测试 | 🟡 部分完成 | 2026-03-02 00:40 | 部分通过 | 命令 1-2 已测试 |
| T4 | 现有 ai-dev 功能回归测试 | ⏸️ 待执行 | - | - | ⚠️ 高优先级 |
| T5 | ai-dev 完整流程测试 | ⏸️ 待执行 | - | - | 可选 |

---

## 测试报告模板

### 测试报告：[测试名称]

**执行时间**：YYYY-MM-DD HH:MM

**执行人**：[姓名]

**测试环境**：
- 操作系统：Windows
- Python 版本：3.x
- 分支：main

**测试结果**：✅ 通过 / ❌ 失败 / ⚠️ 部分通过

**详细记录**：
```
[粘贴测试命令和输出]
```

**问题记录**：
- 问题 1：[描述]
- 问题 2：[描述]

**结论**：
[总结测试结果，是否可以继续下一阶段]

---

## 测试优先级

### 🔥 高优先级（必须通过才能继续）
- T1：目录结构验证
- T2：配置文件验证
- T4：现有 ai-dev 功能回归测试

### ⚠️ 中优先级（建议执行）
- T3：模块加载器功能测试（部分已完成）
- T6：文件迁移完整性测试
- T7：迁移后功能测试

### 💡 低优先级（可选）
- T5：ai-dev 完整流程测试（会创建真实 GitHub 仓库）

---

## 下一步行动

### 立即执行
1. 运行 T1：目录结构验证
2. 运行 T2：配置文件验证
3. 运行 T4：现有 ai-dev 功能回归测试

### 如果测试失败
1. 记录失败原因
2. 回滚更改（如果必要）
3. 修复问题后重新测试

### 如果测试通过
1. 更新测试记录
2. 继续执行阶段 3：文件迁移

---

**文档创建时间**：2026-03-02 00:50  
**文档状态**：待执行测试
