# 测试模块配置：test-and-1
# 用于测试 AND 逻辑条件

# AND 逻辑：目录匹配 AND 文件类型匹配
# 只有当前目录为 .kiro/steering/* 且文件类型为 .md 时才激活
activation_conditions:
  and:
    - directory_match:
        - ".kiro/steering/*"
    - file_type_match:
        - ".md"
